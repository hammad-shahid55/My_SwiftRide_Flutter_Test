import React from 'react'
import ReactDOM from 'react-dom/client'
import FlutterTestPresentation from './App.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FlutterTestPresentation />
  </React.StrictMode>,
)

